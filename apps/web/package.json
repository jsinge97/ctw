{
  "name": "@ctw/web",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "lint": "eslint src",
    "e2e": "vitest run e2e/**/*.spec.ts"
  },
  "dependencies": {
    "@ctw/api-client": "workspace:*",
    "@ctw/contracts": "workspace:*",
    "@ctw/domain": "workspace:*",
    "@ctw/permissions": "workspace:*"
  },
  "devDependencies": {}
}
